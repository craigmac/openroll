#ifndef OPENROLLCONFIG_H
#define OPENROLLCONFIG_H

#include <QString>

const QString MAJOR_VERSION = "2";
const QString MINOR_VERSION = "0";
const QString PATCH_VERSION = "2";

const QString OPENROLL_VERSION = MAJOR_VERSION + "." + MINOR_VERSION + "." + PATCH_VERSION;
#endif // OPENROLLCONFIG_H
